import{describe,expect,it}from'vitest';import{analyze,generate,GenerationError}from'./engine';import{newProject,type Project}from'./types';
const fixture=():Project=>({...newProject(),size:4,layers:[{id:'bg',name:'Sfondo',presence:1,allowNone:false,traits:[{id:'red',name:'Rosso',metadataName:'Rosso',weight:1,enabled:true},{id:'blue',name:'Blu',metadataName:'Blu',weight:1,enabled:true}]},{id:'body',name:'Corpo',presence:1,allowNone:false,traits:[{id:'cat',name:'Gatto',metadataName:'Gatto',weight:1,enabled:true},{id:'bot',name:'Robot',metadataName:'Robot',weight:1,enabled:true}]}],characters:[{id:'hero',name:'Eroe',weight:1,layerIds:['bg','body']} ]});
describe('motore',()=>{it('riproduce seed e DNA unici',()=>{const p=fixture(),a=generate(p),b=generate(p);expect(a.map(x=>x.dna)).toEqual(b.map(x=>x.dna));expect(new Set(a.map(x=>x.dna)).size).toBe(4)});it('rispetta esclusioni',()=>{const p={...fixture(),size:3,rules:[{id:'r',type:'exclude' as const,ifTrait:'red',thenTrait:'cat'}]};expect(generate(p).every(x=>!(x.dna.includes('red')&&x.dna.includes('cat')))).toBe(true)});it('rileva regole contraddittorie',()=>{const p={...fixture(),rules:[{id:'1',type:'exclude' as const,ifTrait:'red',thenTrait:'cat'},{id:'2',type:'require' as const,ifTrait:'red',thenTrait:'cat'}]};expect(analyze(p).contradictions.length).toBe(1);expect(()=>generate(p)).toThrow(GenerationError)});it('inserisce 1/1 riservati',()=>{const p={...fixture(),oneOfOnes:[{tokenId:1,name:'Unico',description:'',attributes:[]}]};expect(generate(p)[0].special).toBe(true)})});
