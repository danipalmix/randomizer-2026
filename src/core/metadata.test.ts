import{expect,it}from'vitest';import{metadata,replaceUriPrefix,validateMetadata}from'./metadata';import{newProject,type Item}from'./types';const item:Item={tokenId:1,name:'NFT #1',characterId:'x',selections:{},dna:'x',attributes:[{trait_type:'Power',value:10,display_type:'boost_number'}]};it('genera EVM mantenendo numeri',()=>{const m=metadata(newProject(),item) as any;expect(m.attributes[0].value).toBe(10);expect(m.image).toContain('/1.png')});it('valida creator Solana',()=>{const p={...newProject(),profile:'solana' as const,creators:[{address:'x',share:90}]};expect(validateMetadata(p,[item])).toContain('Creator: le quote devono sommare a 100')});it('sostituisce prefissi URI',()=>expect(replaceUriPrefix([{image:'ipfs://old/1'}],'ipfs://old','ipfs://new')).toEqual([{image:'ipfs://new/1'}]));
